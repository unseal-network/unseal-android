# Issue tracker: Local Markdown

Issues and PRDs for this repository live as Markdown files under `.scratch/`.

## Conventions

- One feature per directory: `.scratch/<feature-slug>/`.
- The PRD is `.scratch/<feature-slug>/PRD.md`.
- Implementation issues are `.scratch/<feature-slug>/issues/<NN>-<slug>.md`, numbered from `01`.
- Triage state is recorded in a `Status:` line near the top of each issue.
- Comments and implementation history append under a `## Comments` heading.

When a skill says to publish to the issue tracker, create the relevant Markdown artifact under `.scratch/<feature-slug>/`.
